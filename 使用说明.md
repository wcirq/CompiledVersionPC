# 列车顶视觉检测库使用说明

## 1. `export` 目录文件说明

当前 `export` 目录中的文件及作用如下：

- `train_roof_anomaly_store-0.1.0-py3-none-any.whl`
  Python 安装包，用于安装本库。
- `test_store.py`
  `store_core` 使用示例，主要用于演示异物检测模型的训练、检测、按路径保存结果等流程。
- `test_store_infer.py`
  `store_infer` 使用示例，主要用于演示固定模型推理流程，目前示例模型为烟火识别模型 `fire_smoke`。
- `cf177220111084e5238e303fce537a20.jpg`
  示例图片，可用于安装完成后的快速验证。
- `model_18d46afee4db49f9-deploy.zip`
  已导出的异物检测部署模型包，可在前端页面中通过“导入模型”直接导入使用。

说明：

- `使用说明.md` 可在整理交付文件时手动复制到 `export` 目录中
- `model_18d46afee4db49f9-deploy.zip` 用于前端页面导入模型，或在目标环境中恢复异物检测模型
- `test_store.py` 和 `test_store_infer.py` 用于安装完成后的快速验证

建议先按本说明完成安装，再运行示例脚本或导入部署模型进行验证。

---

## 2. 库说明

本库提供两类能力：

- `store_core`
  用于列车顶异物检测模型的训练、管理和检测。
- `store_infer`
  用于调用库内置的固定检测模型做推理，例如烟火识别模型 `fire_smoke`。

功能包括：

- 用一批正常车顶图片训练一个异物检测模型
- 对单张图或整目录图片做异物检测
- 输出热力图、异常框、异常轮廓、批量保存结果
- 启动 Web 管理页面进行训练、检测和样本维护

---

## 3. 安装方式

安装包通常为 wheel 文件，例如：

```bash
train_roof_anomaly_store-0.1.0-py3-none-any.whl
```

### 3.1 纯 CPU 环境

先安装 CPU 版 PyTorch，再安装 wheel：

```bash
pip install torch torchvision
pip install train_roof_anomaly_store-0.1.0-py3-none-any.whl
```

如果运行环境中已经具备可用的 CPU 版 `torch` / `torchvision`，可直接安装 wheel。

### 3.2 CUDA 环境

先根据机器上的 CUDA 版本安装对应的 PyTorch，再安装 wheel。

示例：

```bash
pip install torch torchvision
pip install train_roof_anomaly_store-0.1.0-py3-none-any.whl
```

注意：

- `torch` / `torchvision` 需要与本机 CUDA 环境匹配
- 如果 CUDA 环境可用，异物检测后端会自动优先使用 CUDA

### 3.3 BM 环境

BM 环境除安装 wheel 外，还需要单独准备 Sophon 运行时，例如 `sophon.sail`。

`sophon.sail` 是算能 BM 芯片的 Python 运行库，异物检测在 BM 后端下会通过它调用 BM 模型完成分割、特征提取和向量检索等推理流程。

示例：

```bash
pip install train_roof_anomaly_store-0.1.0-py3-none-any.whl
```

注意：

- `sophon.sail` 不是通过这个 wheel 自动安装的
- BM 运行环境需要提前部署
- 如果 `sophon.sail` 可用，后端会自动优先使用 BM

### 3.4 后端自动选择规则

异物检测相关模块统一按下面顺序自动选择后端：

```text
bm -> cuda -> cpu
```

说明如下：

- 能用 BM 就优先 BM
- BM 不可用时尝试 CUDA
- CUDA 也不可用时回落到 CPU

---

## 4. 示例脚本说明

### 4.1 `test_store.py`

`test_store.py` 用于演示 `store_core` 的完整流程，包括：

- 初始化 `TrainRoofAnomalyStore`
- 训练异物检测模型
- 对单张图做异物检测
- 对一批图片做批量检测
- 调用“按路径检测并保存结果”

常用运行方式：

```bash
python test_store.py
```

可根据脚本底部 `__main__` 中的注释切换需要执行的示例。

### 4.2 `test_store_infer.py`

`test_store_infer.py` 用于演示 `store_infer` 的固定模型推理能力，包括：

- 列出当前可用的内置推理模型
- 运行一个固定模型示例，例如烟火识别模型 `fire_smoke`

运行方式：

```bash
python test_store_infer.py
```

---

## 5. Python 使用示例

### 5.1 异物检测模型训练与检测

```python
from store_core import TrainRoofAnomalyStore
from store_core.schemas import RuntimeOptions

store = TrainRoofAnomalyStore(
    root_dir="./store_data",
    autostart_service=False,
)

options = RuntimeOptions(
    use_segmentation=False,
    enable_tiling=True,
    crop_size=(240, 240),
    stride=(240, 240),
)

train_result = store.train_model(
    model_name="列车顶异物检测模型",
    image_dir="templates2",
    runtime_options=options,
)

detect_result = store.detect_image(
    model_id=train_result["model_id"],
    image_path="test5.jpg",
    include_heatmap_base64=True,
)

print(detect_result)
```

### 5.2 内置模型推理

```python
from store_infer import list_models, run_inference

print(list_models())

result = run_inference(
    model_name="fire_smoke",
    image_path="cf177220111084e5238e303fce537a20.jpg",
    conf_threshold=0.25,
    include_visualization_base64=True,
)

print(result)
```

---

## 6. Web 页面启动

安装完成后可直接启动 Web 管理页面：

```bash
store_web
```

或者：

```bash
python -m store_web
```

默认情况下，页面启动在本机：

```text
http://127.0.0.1:8000
```

浏览器访问地址为：

```text
http://localhost:8000
```

如果部署在服务器上，需要将 `localhost` 替换为服务器实际 IP。

如果需要修改端口，可以在启动时显式指定，例如：

```bash
python -m store_web --host 0.0.0.0 --port 8000
```

例如修改为其他端口：

```bash
python -m store_web --host 0.0.0.0 --port 8080
```

启动后可使用以下功能：

- 训练新模型
- 单图检测
- 按路径批量检测并保存结果
- 追加正样本
- 查看训练进度和日志

### 6.1 前端页面主要功能

前端页面主要分为两部分：

- `异物管理`
- `检测测试`

#### 异物管理

主要用于列车顶异物检测模型的训练、维护和使用，包括：

- 模型列表查看
- 训练新模型
- 导入 / 导出模型
- 查看和修改模型默认阈值
- 删除模型
- 单图异物检测
- 按路径批量检测并保存结果
- 追加正样本
- 查看向量库样本
- 查看训练进度和详细日志

其中“异物检测”区域支持：

- 单图检测
- 按路径检测并保存结果
- 修改检测参数
- 查看热力图和异常结果框

其中“追加正样本”区域支持：

- 自动提取轮廓
- 手工调整轮廓
- 把新增正样本追加到当前模型的向量库

#### 导出部署模型

前端导出的 `model_xxx-deploy.zip` 是部署模型包，用于把已经训练完成的异物检测模型迁移到另一台设备或另一套环境中使用。

该压缩包通常只包含当前生效版本所需的最小部署文件，例如：

- `model.json`
- 当前版本对应的 `version.json`
- 当前版本的模型文件，例如 `memory_model.pt`

与“导出模型”相比，“导出部署模型”不包含训练过程中的全部历史文件，体积通常更小，更适合部署、备份和分发。

#### 导入模型

前端页面支持直接导入 `zip` 模型包。

操作方式如下：

1. 打开前端页面的 `异物管理` 区域。
2. 点击页面顶部的 `导入模型`。
3. 选择导出的模型压缩包，例如 `model_18d46afee4db49f9-deploy.zip`。
4. 导入完成后，模型会出现在模型列表中，可直接用于单图检测、批量检测或继续维护。

说明：

- 导入包必须是系统导出的标准模型压缩包
- 压缩包内应包含完整的模型目录结构
- 若导入包中的 `model_id` 或 `model_name` 与当前系统已有模型重复，系统会提示导入失败

#### 检测测试

主要用于调用 `store_infer` 内置固定模型进行推理测试，包括：

- 选择内置模型
- 上传测试图片
- 调整推理参数
  - `conf_threshold`
  - `iou_threshold`
  - `imgsz`
  - `max_det`
- 查看推理结果和可视化图

该页面更适合验证 `fire_smoke` 这类固定识别模型，不用于训练新的异物检测模型。

---
